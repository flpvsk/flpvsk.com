import styled from '@emotion/styled';

import {
  fontSize,
  color,
  textStyle,
  space,
} from 'styled-system'

const A = styled.a({
    textDecoration: 'none',
  },
  space,
  fontSize,
  textStyle,
  color,
);

export default A;
